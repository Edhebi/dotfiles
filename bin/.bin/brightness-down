#!/bin/bash

KERN="intel_backlight"

FILE=/sys/class/backlight/$KERN/brightness
CURR=$(< $FILE)
MAX=$(< /sys/class/backlight/$KERN/max_brightness)
STEP=$((MAX / 6))

echo -n "$((CURR - STEP > 1 ? CURR - STEP : 0))" > $FILE
